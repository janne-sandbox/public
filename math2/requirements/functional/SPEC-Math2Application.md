# Class specification — Math2Application

`Math2Application` is the Spring Boot entry point. It enables validated Math2 configuration and starts no remotely exposed listener unless the server address is explicitly overridden.
