# Class specification — EquationBatchParser

`EquationBatchParser` separates top-level comma-delimited equations while preserving commas nested in functions or parentheses. Blank batches and empty elements are invalid.
