# Class specification — UncalculableException

`UncalculableException` is the public unchecked base error specified by REG-005. It always carries a `CalculationError` and may carry a non-negative parser position.
