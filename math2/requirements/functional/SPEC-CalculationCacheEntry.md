# Class specification — CalculationCacheEntry

`CalculationCacheEntry` maps one versioned canonical calculation to its unbounded CLOB result, precision, calculation duration in milliseconds, creation time, and mutable last-access time. The cache key and creation metadata are immutable after insertion.
