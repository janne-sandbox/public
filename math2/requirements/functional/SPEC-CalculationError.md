# Class specification — CalculationError

`CalculationError` is the public enum of stable machine-readable error categories listed in REG-005. Adding a value is backward compatible; renaming or removing one is a REST contract change.
