# Class specification — CalculationController

`CalculationController` implements the two GET endpoints in REG-006, preserves batch order, selects default precision, and rejects oversized batches before evaluation.
