# Class specification — Operator

`Operator` is the public abstraction for a symbol, arity, and precision-aware evaluation of immutable operands. Implementations reject the wrong operand count and never mutate inputs.
