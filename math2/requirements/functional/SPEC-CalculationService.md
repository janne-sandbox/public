# Class specification — CalculationService

`CalculationService` validates expression length, canonicalizes and evaluates equations, maintains versioned cache keys, updates access time, and enforces the configured LRU bound transactionally.
